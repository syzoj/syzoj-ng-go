// Code generated by SQLBoiler 3.5.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/pkg/errors"
	"github.com/volatiletech/sqlboiler/boil"
	"github.com/volatiletech/sqlboiler/queries"
	"github.com/volatiletech/sqlboiler/queries/qm"
	"github.com/volatiletech/sqlboiler/queries/qmhelper"
	"github.com/volatiletech/sqlboiler/strmangle"
)

// SubmissionStatistic is an object representing the database table.
type SubmissionStatistic struct {
	ProblemID    int    `boil:"problem_id" json:"problem_id" toml:"problem_id" yaml:"problem_id"`
	UserID       int    `boil:"user_id" json:"user_id" toml:"user_id" yaml:"user_id"`
	Type         string `boil:"type" json:"type" toml:"type" yaml:"type"`
	Key          int    `boil:"key" json:"key" toml:"key" yaml:"key"`
	SubmissionID int    `boil:"submission_id" json:"submission_id" toml:"submission_id" yaml:"submission_id"`

	R *submissionStatisticR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L submissionStatisticL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var SubmissionStatisticColumns = struct {
	ProblemID    string
	UserID       string
	Type         string
	Key          string
	SubmissionID string
}{
	ProblemID:    "problem_id",
	UserID:       "user_id",
	Type:         "type",
	Key:          "key",
	SubmissionID: "submission_id",
}

// Generated where

var SubmissionStatisticWhere = struct {
	ProblemID    whereHelperint
	UserID       whereHelperint
	Type         whereHelperstring
	Key          whereHelperint
	SubmissionID whereHelperint
}{
	ProblemID:    whereHelperint{field: "`submission_statistics`.`problem_id`"},
	UserID:       whereHelperint{field: "`submission_statistics`.`user_id`"},
	Type:         whereHelperstring{field: "`submission_statistics`.`type`"},
	Key:          whereHelperint{field: "`submission_statistics`.`key`"},
	SubmissionID: whereHelperint{field: "`submission_statistics`.`submission_id`"},
}

// SubmissionStatisticRels is where relationship names are stored.
var SubmissionStatisticRels = struct {
}{}

// submissionStatisticR is where relationships are stored.
type submissionStatisticR struct {
}

// NewStruct creates a new relationship struct
func (*submissionStatisticR) NewStruct() *submissionStatisticR {
	return &submissionStatisticR{}
}

// submissionStatisticL is where Load methods for each relationship are stored.
type submissionStatisticL struct{}

var (
	submissionStatisticAllColumns            = []string{"problem_id", "user_id", "type", "key", "submission_id"}
	submissionStatisticColumnsWithoutDefault = []string{"problem_id", "user_id", "type", "key", "submission_id"}
	submissionStatisticColumnsWithDefault    = []string{}
	submissionStatisticPrimaryKeyColumns     = []string{"problem_id", "user_id", "type"}
)

type (
	// SubmissionStatisticSlice is an alias for a slice of pointers to SubmissionStatistic.
	// This should generally be used opposed to []SubmissionStatistic.
	SubmissionStatisticSlice []*SubmissionStatistic
	// SubmissionStatisticHook is the signature for custom SubmissionStatistic hook methods
	SubmissionStatisticHook func(context.Context, boil.ContextExecutor, *SubmissionStatistic) error

	submissionStatisticQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	submissionStatisticType                 = reflect.TypeOf(&SubmissionStatistic{})
	submissionStatisticMapping              = queries.MakeStructMapping(submissionStatisticType)
	submissionStatisticPrimaryKeyMapping, _ = queries.BindMapping(submissionStatisticType, submissionStatisticMapping, submissionStatisticPrimaryKeyColumns)
	submissionStatisticInsertCacheMut       sync.RWMutex
	submissionStatisticInsertCache          = make(map[string]insertCache)
	submissionStatisticUpdateCacheMut       sync.RWMutex
	submissionStatisticUpdateCache          = make(map[string]updateCache)
	submissionStatisticUpsertCacheMut       sync.RWMutex
	submissionStatisticUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var submissionStatisticBeforeInsertHooks []SubmissionStatisticHook
var submissionStatisticBeforeUpdateHooks []SubmissionStatisticHook
var submissionStatisticBeforeDeleteHooks []SubmissionStatisticHook
var submissionStatisticBeforeUpsertHooks []SubmissionStatisticHook

var submissionStatisticAfterInsertHooks []SubmissionStatisticHook
var submissionStatisticAfterSelectHooks []SubmissionStatisticHook
var submissionStatisticAfterUpdateHooks []SubmissionStatisticHook
var submissionStatisticAfterDeleteHooks []SubmissionStatisticHook
var submissionStatisticAfterUpsertHooks []SubmissionStatisticHook

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *SubmissionStatistic) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range submissionStatisticBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *SubmissionStatistic) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range submissionStatisticBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *SubmissionStatistic) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range submissionStatisticBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *SubmissionStatistic) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range submissionStatisticBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *SubmissionStatistic) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range submissionStatisticAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterSelectHooks executes all "after Select" hooks.
func (o *SubmissionStatistic) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range submissionStatisticAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *SubmissionStatistic) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range submissionStatisticAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *SubmissionStatistic) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range submissionStatisticAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *SubmissionStatistic) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range submissionStatisticAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddSubmissionStatisticHook registers your hook function for all future operations.
func AddSubmissionStatisticHook(hookPoint boil.HookPoint, submissionStatisticHook SubmissionStatisticHook) {
	switch hookPoint {
	case boil.BeforeInsertHook:
		submissionStatisticBeforeInsertHooks = append(submissionStatisticBeforeInsertHooks, submissionStatisticHook)
	case boil.BeforeUpdateHook:
		submissionStatisticBeforeUpdateHooks = append(submissionStatisticBeforeUpdateHooks, submissionStatisticHook)
	case boil.BeforeDeleteHook:
		submissionStatisticBeforeDeleteHooks = append(submissionStatisticBeforeDeleteHooks, submissionStatisticHook)
	case boil.BeforeUpsertHook:
		submissionStatisticBeforeUpsertHooks = append(submissionStatisticBeforeUpsertHooks, submissionStatisticHook)
	case boil.AfterInsertHook:
		submissionStatisticAfterInsertHooks = append(submissionStatisticAfterInsertHooks, submissionStatisticHook)
	case boil.AfterSelectHook:
		submissionStatisticAfterSelectHooks = append(submissionStatisticAfterSelectHooks, submissionStatisticHook)
	case boil.AfterUpdateHook:
		submissionStatisticAfterUpdateHooks = append(submissionStatisticAfterUpdateHooks, submissionStatisticHook)
	case boil.AfterDeleteHook:
		submissionStatisticAfterDeleteHooks = append(submissionStatisticAfterDeleteHooks, submissionStatisticHook)
	case boil.AfterUpsertHook:
		submissionStatisticAfterUpsertHooks = append(submissionStatisticAfterUpsertHooks, submissionStatisticHook)
	}
}

// One returns a single submissionStatistic record from the query.
func (q submissionStatisticQuery) One(ctx context.Context, exec boil.ContextExecutor) (*SubmissionStatistic, error) {
	o := &SubmissionStatistic{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for submission_statistics")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all SubmissionStatistic records from the query.
func (q submissionStatisticQuery) All(ctx context.Context, exec boil.ContextExecutor) (SubmissionStatisticSlice, error) {
	var o []*SubmissionStatistic

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to SubmissionStatistic slice")
	}

	if len(submissionStatisticAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all SubmissionStatistic records in the query.
func (q submissionStatisticQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count submission_statistics rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q submissionStatisticQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if submission_statistics exists")
	}

	return count > 0, nil
}

// SubmissionStatistics retrieves all the records using an executor.
func SubmissionStatistics(mods ...qm.QueryMod) submissionStatisticQuery {
	mods = append(mods, qm.From("`submission_statistics`"))
	return submissionStatisticQuery{NewQuery(mods...)}
}

// FindSubmissionStatistic retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindSubmissionStatistic(ctx context.Context, exec boil.ContextExecutor, problemID int, userID int, type_ string, selectCols ...string) (*SubmissionStatistic, error) {
	submissionStatisticObj := &SubmissionStatistic{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `submission_statistics` where `problem_id`=? AND `user_id`=? AND `type`=?", sel,
	)

	q := queries.Raw(query, problemID, userID, type_)

	err := q.Bind(ctx, exec, submissionStatisticObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from submission_statistics")
	}

	return submissionStatisticObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *SubmissionStatistic) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no submission_statistics provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(submissionStatisticColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	submissionStatisticInsertCacheMut.RLock()
	cache, cached := submissionStatisticInsertCache[key]
	submissionStatisticInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			submissionStatisticAllColumns,
			submissionStatisticColumnsWithDefault,
			submissionStatisticColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(submissionStatisticType, submissionStatisticMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(submissionStatisticType, submissionStatisticMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `submission_statistics` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `submission_statistics` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `submission_statistics` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, submissionStatisticPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into submission_statistics")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.ProblemID,
		o.UserID,
		o.Type,
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.retQuery)
		fmt.Fprintln(boil.DebugWriter, identifierCols...)
	}

	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for submission_statistics")
	}

CacheNoHooks:
	if !cached {
		submissionStatisticInsertCacheMut.Lock()
		submissionStatisticInsertCache[key] = cache
		submissionStatisticInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the SubmissionStatistic.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *SubmissionStatistic) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	submissionStatisticUpdateCacheMut.RLock()
	cache, cached := submissionStatisticUpdateCache[key]
	submissionStatisticUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			submissionStatisticAllColumns,
			submissionStatisticPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update submission_statistics, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `submission_statistics` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, submissionStatisticPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(submissionStatisticType, submissionStatisticMapping, append(wl, submissionStatisticPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update submission_statistics row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for submission_statistics")
	}

	if !cached {
		submissionStatisticUpdateCacheMut.Lock()
		submissionStatisticUpdateCache[key] = cache
		submissionStatisticUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q submissionStatisticQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for submission_statistics")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for submission_statistics")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o SubmissionStatisticSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), submissionStatisticPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `submission_statistics` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, submissionStatisticPrimaryKeyColumns, len(o)))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in submissionStatistic slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all submissionStatistic")
	}
	return rowsAff, nil
}

var mySQLSubmissionStatisticUniqueColumns = []string{}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *SubmissionStatistic) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no submission_statistics provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(submissionStatisticColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLSubmissionStatisticUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	submissionStatisticUpsertCacheMut.RLock()
	cache, cached := submissionStatisticUpsertCache[key]
	submissionStatisticUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			submissionStatisticAllColumns,
			submissionStatisticColumnsWithDefault,
			submissionStatisticColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			submissionStatisticAllColumns,
			submissionStatisticPrimaryKeyColumns,
		)

		if len(update) == 0 {
			return errors.New("models: unable to upsert submission_statistics, could not build update column list")
		}

		ret = strmangle.SetComplement(ret, nzUniques)
		cache.query = buildUpsertQueryMySQL(dialect, "submission_statistics", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `submission_statistics` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(submissionStatisticType, submissionStatisticMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(submissionStatisticType, submissionStatisticMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for submission_statistics")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(submissionStatisticType, submissionStatisticMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for submission_statistics")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.retQuery)
		fmt.Fprintln(boil.DebugWriter, nzUniqueCols...)
	}

	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for submission_statistics")
	}

CacheNoHooks:
	if !cached {
		submissionStatisticUpsertCacheMut.Lock()
		submissionStatisticUpsertCache[key] = cache
		submissionStatisticUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single SubmissionStatistic record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *SubmissionStatistic) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no SubmissionStatistic provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), submissionStatisticPrimaryKeyMapping)
	sql := "DELETE FROM `submission_statistics` WHERE `problem_id`=? AND `user_id`=? AND `type`=?"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from submission_statistics")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for submission_statistics")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q submissionStatisticQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no submissionStatisticQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from submission_statistics")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for submission_statistics")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o SubmissionStatisticSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(submissionStatisticBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), submissionStatisticPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `submission_statistics` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, submissionStatisticPrimaryKeyColumns, len(o))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args)
	}

	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from submissionStatistic slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for submission_statistics")
	}

	if len(submissionStatisticAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *SubmissionStatistic) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindSubmissionStatistic(ctx, exec, o.ProblemID, o.UserID, o.Type)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *SubmissionStatisticSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := SubmissionStatisticSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), submissionStatisticPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `submission_statistics`.* FROM `submission_statistics` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, submissionStatisticPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in SubmissionStatisticSlice")
	}

	*o = slice

	return nil
}

// SubmissionStatisticExists checks if the SubmissionStatistic row exists.
func SubmissionStatisticExists(ctx context.Context, exec boil.ContextExecutor, problemID int, userID int, type_ string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `submission_statistics` where `problem_id`=? AND `user_id`=? AND `type`=? limit 1)"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, problemID, userID, type_)
	}

	row := exec.QueryRowContext(ctx, sql, problemID, userID, type_)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if submission_statistics exists")
	}

	return exists, nil
}
